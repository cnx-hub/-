import { View } from "@tarojs/components";

import { styled } from "linaria/react";

export const DetailSongWrapper = styled(View)`
  padding: 0 20px;

  .search {
    position: fixed;
    top: 0;
    left: 20rpx;
    right: 20rpx;
  }
`;
